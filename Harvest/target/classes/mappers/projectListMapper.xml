<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- Mapper.xml 만들 때 참고용, 아래 namespace 수정해서 쓰세요! -->
<mapper namespace="com.itwillbs.mappers.projectListMapper">

	<select id="getPjList" resultType="com.itwillbs.domain.ProjectDTO">
		SELECT IDX
			 , CATEGORY
			 , CRE_NM
			 , TITLE
			 , TARGET_AMT
			 , START
			 , END
			 , IMG1
		  FROM PROJECT_TB
		 WHERE 1=1
		 <if test="value='search'">
		   and title like CONCAT('%', #{search}, '%')
		 </if>
<!-- 		 <if test="category"> -->
<!-- 		   and category=#{category} -->
<!-- 		 </if>  -->
		 AND END > CURDATE()
	</select>
	
	

	<select id="getCateList" resultType="com.itwillbs.domain.ProjectDTO">
		select * 
		  from project_tb 
		 where category=#{category} and END > CURDATE()
	</select>
	
	<select id="getCateCount" resultType="java.lang.Integer">
		select count(*) from project_tb where category=#{category} and END > CURDATE()
	</select>
	
	
	<select id="getProjectList" parameterType="map" resultType="com.itwillbs.domain.ProjectDTO">
		SELECT IDX
			 , CATEGORY
			 , CRE_NM
			 , TITLE
			 , TARGET_AMT
			 , TOTAL_AMT
			 , START
			 , END
			 , IMG1
		 	 , case when (SELECT COUNT(*)
					    	FROM LIKE_TB
					   	   WHERE PJ_IDX = P.IDX
							 AND USER_ID = #{SESSIONID}) = 1 THEN 'heart_fill.svg'
			   											 	 ELSE 'heart.svg'
			   											 	  END AS HEART  
		  FROM PROJECT_TB P
		 WHERE 1=1
		 <if test='! GB.equals("") and GB != null'>
		 	  <if test='GB.equals("POP")'>  
		   	  AND END > CURDATE()
			  </if>
			  <if test='GB.equals("NEW")'>  
		   	  AND START > DATE_SUB(CURDATE(),INTERVAL 7 day) and END > CURDATE()
			  </if>
		  	  <if test='GB.equals("DEAD")'>  
		   	  AND CURDATE() > DATE_SUB(END,INTERVAL 7 day) and END > CURDATE()
			  </if>
			  <if test='GB.equals("EXP")'>  
		   	  AND START > CURDATE()
			  </if>
		 </if>
<!-- 		 <if test='! search.equals("") and search != null'> -->
<!-- 			  AND TITLE LIKE CONCAT('%', #{search}, '%') -->
<!-- 		 </if> -->
	</select>

	<select id="getCount" parameterType="map" resultType="java.lang.Integer">
		SELECT COUNT(*)
		  FROM PROJECT_TB 
		 WHERE 1=1
		 <if test='! GB.equals("") and GB != null'>
		  	  <if test='GB.equals("DEAD")'>  
		   	  AND CURDATE() > DATE_SUB(END,INTERVAL 7 day) AND END > CURDATE()
			  </if>
			  <if test='GB.equals("EXP")'>  
		   	  AND START > CURDATE()
			  </if>
			  <if test='GB.equals("POP")'>  
		   	  AND END > CURDATE()
			  </if>
			  <if test='GB.equals("NEW")'>  
		   	  AND START > DATE_SUB(CURDATE(),INTERVAL 7 day) and END > CURDATE()
			  </if>
<!-- 			  <if test='GB.equals(search)'> -->
<!-- 			  AND TITLE LIKE CONCAT('%', #{search}, '%') -->
<!-- 			  </if> -->
			  
		 </if>
	</select>
	
	<select id="getPjCount" resultType="java.lang.Integer">
		SELECT COUNT(*)
		  FROM PROJECT_TB
		 WHERE 1=1
		 <if test="value='search'">
		   and title like CONCAT('%', #{search}, '%')
		 </if>
<!-- 		 <if test="category"> -->
<!-- 		   and category=#{category} -->
<!-- 		 </if>  -->
		 AND END > CURDATE()
	</select>
	
	

	
	<select id="updateAlramcount">
		update project_tb set alramcount=alramcount+1 where idx=#{idx}
	</select>
	
	
</mapper>  